import type { ChatMessage, ChatSession } from "@/types/chat.types";

// All chat-related database operations are now handled via API routes.
// This file remains for type definitions if they are not moved elsewhere.
