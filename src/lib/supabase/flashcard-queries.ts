import type { Flashcard, FlashcardSet } from "@/types/flashcard.types";

// All flashcard-related database operations are now handled via API routes.
// This file remains for type definitions if they are not moved elsewhere.
