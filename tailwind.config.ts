import type { Config } from "tailwindcss";

export default {
  theme: {
    extend: {
      colors: {
        background: "#0a0a0f",
        foreground: "#e5e5e5",
      },
    },
  },
} satisfies Config;